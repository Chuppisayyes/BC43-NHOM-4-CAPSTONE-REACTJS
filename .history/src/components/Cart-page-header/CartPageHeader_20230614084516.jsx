import React from 'react'
import { connect } from 'react-redux'
 
const CartPageHeader = (props) => {
  return (
    <div>CartPageHeader</div>
  )
}

const mapStateToProps = (state) => ({})


export default connect(mapStateToProps, mapDispatchToProps)(CartPageHeader)