import React from 'react'
import { connect } from 'react-redux'

 const CartPageFooter = (props) => {
  return (
    <div>CartPageFooter</div>
  )
}

const mapStateToProps = (state) => ({})
export default connect(mapStateToProps)(CartPageFooter)